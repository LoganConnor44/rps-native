import * as React from 'react';
import { View } from 'react-native';
import separator from '../styles/separator';

const Separator = () => (
    <View style={separator.main} />
);

export default Separator